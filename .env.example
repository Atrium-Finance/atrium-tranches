# Coverage runs (existing)
COVERAGE=false

# ---- Fork tests (Arbitrum mainnet) ----
# Set to "true" to enable the test/fork/** suite. Tests skip themselves
# when this is unset or ARBITRUM_RPC_URL is missing.
FORK_TESTS=false

# Alchemy / Infura / Ankr Arbitrum mainnet RPC. Free tiers work but may
# rate-limit on long suites — production runs use a paid endpoint.
ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/YOUR_KEY_HERE

# Optional — pin a specific block number, or "latest" to follow the tip.
# Pinned blocks make tests reproducible. Update quarterly or after Aave /
# USD.AI contract upgrades.
FORK_BLOCK_NUMBER=250000000

# ---- Deployment (Arbitrum One) ----
# Deployer EOA private key (0x-prefixed). Must be funded with ETH on
# Arbitrum and must match `owner` in ignition/parameters/mainnet.json.
# Reuses ARBITRUM_RPC_URL above for the `arbitrum` network + E2E scripts.
DEPLOYER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Etherscan V2 multichain API key (covers Arbiscan). ARBISCAN_API_KEY is
# accepted as a fallback alias.
ETHERSCAN_API_KEY=YOUR_KEY
# ARBISCAN_API_KEY=YOUR_KEY

# Ignition required confirmations. Live Arbitrum uses the default of 3.
# Set to 1 for fork dry-runs — an auto-mining EDR fork produces no new
# blocks after the last tx in a batch, so >1 confirmation stalls.
# IGNITION_CONFIRMATIONS=1

# ---- E2E scripts (pnpm e2e:deposit / e2e:withdraw) ----
# Network the E2E scripts connect to (default: arbitrum). Set to a local
# fork network name when dry-running.
# ATRIUM_NETWORK=arbitrum

# Deposit/withdraw size (whole tokens, 18-decimals) for the E2E scripts.
# Default: 10.
# E2E_AMOUNT=10
