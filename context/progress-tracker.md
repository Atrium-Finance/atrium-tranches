# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 1: Tranche foundations

## Current Goal

- Flesh out the `PrimeCDO` implementation against `ICDO` — accounting, withdraw routing, and remaining views. Component wiring (`config(...)`) is in place and now also primes Tranche → Strategy allowances; `deposit(...)` routing is live. `PrimeCDO` is on the shared `AccessControlled` base (spec 07a) and now carries per-tranche pause flags (`TActionState` per Junior/Mezz/Senior, default disabled, gated by `PAUSER_ROLE`) via spec 07b. Remaining `ICDO` surface (`updateAccounting`, `totalAssets`, `maxDeposit`, `maxWithdraw`, `withdraw`) is stubbed and will be replaced one spec at a time.

## Completed

- **01 — Create Tranche interface & base contract**
  - Added `contracts/interfaces/ITranche.sol` extending `IERC4626`.
  - Added `contracts/vaults/Tranche.sol` inheriting `Initializable`, `ERC4626Upgradeable`, and implementing `ITranche`, with an `initialize(IERC20, string, string)` initializer.
  - Installed `@openzeppelin/contracts-upgradeable` (`5.6.1`).
  - `pnpm build` compiles successfully under solc 0.8.28.
- **02 — Create CDO interface**
  - Added `contracts/interfaces/ICDO.sol` with tranche vault getters (`jrVault`, `mezzVault`, `srVault`), accounting (`totalAssets(address)`, `updateAccounting()`), deposit/withdraw routing, and limit queries (`maxWithdraw` overloads, `maxDeposit`).
  - Explicit named import of `ITranche` from `./ITranche.sol`.
- **03 — Override ERC4626 deposit & withdraw flows**
  - Overrode the four state-changing ERC4626 entrypoints on `Tranche` (`deposit(uint256,address)`, `mint(uint256,address)`, `withdraw(uint256,address,address)`, `redeem(uint256,address,address)`) to call `cdo.updateAccounting()` before delegating to `super.*`.
  - Each override declares `override(ERC4626Upgradeable, IERC4626)` since the functions are inherited from both bases; added a named import for `IERC4626`.
  - Overrode the internal `_deposit` hook so that after `super._deposit` (which pulls assets and mints shares) the tranche forwards the assets to the CDO via `cdo.deposit(address(this), asset(), assets, assets)`. Centralizes the forwarding for both `deposit` and `mint` paths.
  - The token-routed `IPrimeVault` overloads remain `NotImplemented()` stubs — out of scope per the spec.
  - `pnpm build` compiles cleanly under solc 0.8.35 (only the pre-existing pure-warning lint on the four token-routed stubs remains).
- **PrimeCDO scaffold**
  - Added `contracts/PrimeCDO.sol`: `Initializable`-based contract `PrimeCDO is ICDO`. All ten ICDO functions (`totalAssets`, `updateAccounting`, `deposit`, `withdraw`, both `maxWithdraw` overloads, `maxDeposit`) are stubbed to `revert NotImplemented()` — no business logic yet, per request.
  - Storage: public `jrVault`, `mezzVault`, `srVault` (`ITranche`) and `strategy` (`IStrategy`) — satisfies the four `ICDO` getters automatically. Reserved a `uint256[46]` storage gap (4 slots already used by the four addresses).
  - No initializer at this stage — wiring of `jrVault`/`mezzVault`/`srVault`/`strategy` will happen through dedicated config functions (TBD). `Initializable` is still inherited so future config setters can use `initializer`/`reinitializer` guards.
  - `pnpm build` compiles cleanly under solc 0.8.35.
- **04 — Implement `IPrimeVault` deposit & mint flows**
  - Replaced the `deposit(address,uint256,address)` and `mint(address,uint256,address)` stubs on `contracts/vaults/Tranche.sol` with real bodies.
  - Native asset path (`token == asset()`) delegates to the existing `deposit(uint256,address)` / `mint(uint256,address)` overrides (which already synchronize accounting and forward via the 4-arg `_deposit`).
  - Meta-vault path: synchronizes accounting (`cdo.updateAccounting()`), converts via `cdo.strategy().convertToAssets(..., Math.Rounding.Floor)` for `deposit` or `convertToTokens(..., Math.Rounding.Ceil)` for `mint`, then calls the new 6-arg `_deposit(token, caller, receiver, baseAssets, tokenAssets, shares)`.
  - New internal 6-arg `_deposit` validates source-vault withdraw capacity via `IERC4626(token).maxWithdraw(caller) >= baseAssets`, pulls vault tokens with `SafeERC20.safeTransferFrom`, mints shares to `receiver`, forwards tokens to the CDO (`cdo.deposit(address(this), token, tokenAssets, baseAssets)`), and emits both `Deposit(caller, receiver, baseAssets, shares)` and the new `OnMetaDeposit(receiver, token, tokenAssets, shares)` event.
  - Side-effect additions required to compile: added `function strategy() external view returns (IStrategy);` to `contracts/interfaces/ICDO.sol`; created `contracts/interfaces/IStrategy.sol` with minimal `convertToAssets(address,uint256,Math.Rounding)` and `convertToTokens(address,uint256,Math.Rounding)` getters (full strategy surface deferred).
  - Imports added to `Tranche.sol`: `SafeERC20`, `Math`. `OnMetaDeposit` event is declared locally on `Tranche` for now (no shared interface yet).
  - `pnpm build` compiles cleanly under solc 0.8.35. The only remaining warnings are the pre-existing "state mutability can be restricted to pure" notes on the two remaining `withdraw(address,...)` / `redeem(address,...)` stubs.
- **05 — Create PrimeCDO skeleton & component wiring**
  - Added `contracts/interfaces/IAccounting.sol`: minimal interface with `updateAccounting()` and `totalAssets(address)` only (full accounting surface deferred). Mirrors the `IStrategy` minimality pattern from spec 04.
  - Replaced `contracts/PrimeCDO.sol` with `contracts/core/PrimeCDO.sol`. New contract inherits `Initializable, OwnableUpgradeable, ICDO` (not `CDOComponent` — `PrimeCDO` is the CDO, not a component).
  - Storage layout: `internal` backing fields `_jrVault`, `_mezzVault`, `_srVault` (`ITranche`), `_accounting` (`IAccounting`), `_strategy` (`IStrategy`), and a `uint256[50] __gap`. Public getters `jrVault()`, `mezzVault()`, `srVault()`, `strategy()`, `accounting()` expose them — the first four satisfy `ICDO` directly; `accounting()` is new (not yet on `ICDO`).
  - Constructor calls `_disableInitializers()` per the upgradeability model. New `initialize(address owner_)` is `initializer`-gated and reverts `ZeroAddress()` on a null owner, then calls `__Ownable_init(owner_)`.
  - Atomic `config(address jr, address mz, address sr, address accounting_, address strategy_)` is `onlyOwner` and performs five zero-address checks then five back-reference checks (`ICDOComponent(component).getCDOAddress() == address(this)`) before writing storage. Reverts with `ZeroAddress()` or `InvalidComponent(component, expectedCDO, actualCDO)` on failure, all-or-nothing. May be called multiple times to re-wire. Emits `Configured(jr, mz, sr, accounting_, strategy_)` (three indexed tranche addresses + two non-indexed component addresses, per the 3-indexed cap).
  - Helpers: internal `_requireNonZero(address)` (pure) and `_requireBackRef(address)` (view).
  - All other `ICDO` functions (`totalAssets`, `updateAccounting`, `deposit`, `withdraw`, both `maxWithdraw` overloads, `maxDeposit`) revert `NotImplemented()`. No string reverts; three custom errors total (`NotImplemented`, `ZeroAddress`, `InvalidComponent`).
  - Deleted the previous root-level `contracts/PrimeCDO.sol` scaffold (replaced by the new `core/` location).
  - `pnpm build` compiles cleanly under solc 0.8.35. Only pre-existing pure-mutability warnings remain — now on the seven stubs in `PrimeCDO.sol` and the two remaining `withdraw(address,...)` / `redeem(address,...)` stubs in `Tranche.sol`.
- **06 — Implement PrimeCDO deposit flow**
  - Extended `contracts/interfaces/IStrategy.sol`: appended `deposit(address from, address token, uint256 amount)` and `getSupportedTokens() returns (IERC20[] memory)`. Added `IERC20` named import. Existing `convertToAssets` / `convertToTokens` unchanged.
  - Extended `contracts/interfaces/ITranche.sol`: added `configure() external` (declared `onlyCDO` semantics in NatSpec).
  - Implemented `Tranche.configure()` in `contracts/vaults/Tranche.sol` — `onlyCDO`-gated, reads `address(cdo.strategy())`, iterates `IStrategy(strategy).getSupportedTokens()`, and `SafeERC20.forceApprove`s each token to the strategy at `type(uint256).max`. Loop counter `i` incremented under `unchecked` (bounded by Strategy-admin-controlled list). Added `IStrategy` named import.
  - `contracts/core/PrimeCDO.sol`:
    - Inheritance: added reentrancy protection. Spec called for `ReentrancyGuardUpgradeable` from `@openzeppelin/contracts-upgradeable/utils/ReentrancyGuardUpgradeable.sol`, but **that file does not exist in OZ 5.6.1 contracts-upgradeable** (the package no longer ships an upgradeable variant). Substituted the non-upgradeable `ReentrancyGuard` from `@openzeppelin/contracts/utils/ReentrancyGuard.sol`, which is proxy-safe in OZ 5.x because it uses ERC-7201 namespaced storage and has no init step. Inheritance is now `Initializable, OwnableUpgradeable, ReentrancyGuard, ICDO`. `__gap` left at 50 (no new own-layout slots).
    - `initialize(address)` no longer calls `__ReentrancyGuard_init()` — the namespaced-storage variant has no initializer. Inline comment explains why; storage slot starts at zero, which the modifier correctly treats as "not entered".
    - `config(...)` now calls `_jrVault.configure()`, `_mezzVault.configure()`, `_srVault.configure()` after writing storage and before emitting `Configured`. Order preserves the invariant that `_strategy` is set before `configure()` reads `cdo.strategy()`.
    - Replaced the `NotImplemented()` deposit stub with the real body: `external override onlyTranche nonReentrant`. Validates `token` against `_strategy.getSupportedTokens()` via new `_requireSupported(address)` helper, then forwards via `_strategy.deposit(msg.sender, token, tokenAmount)`. `tranche` and `baseAssets` parameters are accepted (interface-required) but intentionally unused; named-statement trick (`tranche; baseAssets;`) silences unused-warnings. No CDO event emitted (Tranche / Strategy own their event surfaces).
    - New custom errors: `UnauthorizedTranche(address caller)`, `TokenNotSupported(address token)`. New modifier `onlyTranche` rejects any `msg.sender` that is not one of the three wired tranche addresses; pre-config the three storage fields are zero, so the modifier rejects everything until `config(...)` runs.
    - Added `IERC20` named import.
  - `pnpm build` compiles cleanly under solc 0.8.35. Remaining warnings are pre-existing pure-mutability lints on the six other `PrimeCDO` stubs (`totalAssets`, `updateAccounting`, `withdraw`, both `maxWithdraw` overloads, `maxDeposit`) and the two `Tranche` `withdraw(address,...)` / `redeem(address,...)` stubs.
- **07a — Migrate PrimeCDO to AccessControlled**
  - Ported `contracts/interfaces/IAccessControlManager.sol` verbatim from the Strata source. Changes: pragma bumped `^0.8.28` → `0.8.35`, license switched `UNLICENSED` → `BUSL-1.1`. Extends `IAccessControl` from `@openzeppelin/contracts/access/IAccessControl.sol`; declares `grantCall`, `revokeCall`, `isAllowedToCall`, `hasPermission`.
  - Added `contracts/governance/AccessControlled.sol` (ported verbatim from Strata). Changes: pragma → `0.8.35`, license → `BUSL-1.1`, import path for `IAccessControlManager` → `../interfaces/IAccessControlManager.sol`, and one substitution: `ReentrancyGuardUpgradeable` from `@openzeppelin/contracts-upgradeable/utils/` does NOT exist in OZ 5.6.1 (same issue documented for spec 06) — replaced with the non-upgradeable `ReentrancyGuard` from `@openzeppelin/contracts/utils/ReentrancyGuard.sol`, and the `__ReentrancyGuard_init()` call removed from `AccessControlled_init`. All seven role constants preserved (`PAUSER_ROLE`, `UPDATER_CDO_APR_ROLE`, `UPDATER_FEED_ROLE`, `UPDATER_STRAT_CONFIG_ROLE`, `RESERVE_MANAGER_ROLE`, `COOLDOWN_WORKER_ROLE`, `PROPOSER_CONFIG_ROLE`). Storage gap preserved at `uint256[48]` (acm + twoStepConfigManager consume 2 slots from a 50-slot block).
  - Amended `contracts/core/PrimeCDO.sol`:
    - Inheritance: `Initializable, OwnableUpgradeable, ReentrancyGuard, ICDO` → `AccessControlled, ICDO`. Removed `Initializable`, `OwnableUpgradeable`, `ReentrancyGuard` imports; added `AccessControlled` import.
    - Removed the local constructor (`_disableInitializers()` now inherited from `AccessControlled`).
    - Removed the local `error ZeroAddress();` (now inherited from `AccessControlled`); `_requireNonZero` continues to compile against the inherited symbol — no bytecode behavior change.
    - `initialize(address owner_)` → `initialize(address owner_, address acm_)`. Body collapsed to a single `AccessControlled_init(owner_, acm_)` call. The explicit `owner_ == address(0)` check is gone — `Ownable2StepUpgradeable.__Ownable_init_unchained` reverts on zero owner with `OwnableInvalidOwner(address)`; `acm_ == address(0)` reverts with the inherited `ZeroAddress` from `setAccessControlManagerInner`.
    - Added a NatSpec `@dev` storage-layout block documenting the post-07a baseline (`Initializable`, `Ownable`, `Ownable2Step`, `ReentrancyGuard` (ERC-7201 namespaced, no sequential slots), `AccessControlled` (acm + twoStepConfigManager + __gap[48]), `PrimeCDO` own (5 slots + __gap[50])).
    - `config(...)` still `onlyOwner`, body unchanged. `deposit(...)` still `onlyTranche nonReentrant` (resolved to the modifier inherited via `AccessControlled` → `ReentrancyGuard`), body unchanged. All other `ICDO` functions still revert `NotImplemented()`.
    - Remaining errors on `PrimeCDO`: `NotImplemented`, `InvalidComponent`, `UnauthorizedTranche`, `TokenNotSupported`.
  - `pnpm build` compiles cleanly under solc 0.8.35. Only pre-existing pure-mutability lints on the six `PrimeCDO` stubs remain (and the two `Tranche` withdraw/redeem stubs in other files).
  - This is a **storage-layout-breaking** change. Acceptable because no `PrimeCDO` proxy is live yet; the new layout is now the upgrade baseline.
- **07b — Implement PrimeCDO pause (TActionState per tranche)**
  - Amended `contracts/core/PrimeCDO.sol` only. No new files, no interface changes.
  - Declared `enum TrancheKind { JUNIOR, MEZZANINE, SENIOR }` and `struct TActionState { bool isDepositEnabled; bool isWithdrawEnabled; }` at the top of `PrimeCDO`. Two bools pack into a single storage slot per tranche.
  - Added three `public` storage variables `actionsJr`, `actionsMezz`, `actionsSr` between `_strategy` and `__gap`. Auto-generated getters expose both bools for off-chain monitoring. `__gap` shrunk `50` → `47` (three new struct slots) to keep PrimeCDO's reserved-layout total at 50.
  - Added two events: `DepositsStateChanged(address indexed tranche, bool enabled)` and `WithdrawalsStateChanged(address indexed tranche, bool enabled)`. Emitted only when the corresponding flag actually flips (idempotent at flag granularity).
  - Added three custom errors: `InvalidTranche(address tranche)`, `DepositsDisabled(address tranche)`, `WithdrawalsDisabled(address tranche)`. Existing errors unchanged.
  - Added internal helpers `_kindOf(address) → TrancheKind` (three-way address compare, reverts `InvalidTranche` for unwired addresses) and `_actionsOf(address) → TActionState storage` (returns a storage pointer; itself `view` since it performs no writes — Solidity 0.8.35 accepts a view function returning a `storage` reference as long as the function itself doesn't mutate).
  - Added external admin `setActionStates(address tranche, bool isDepositEnabled, bool isWithdrawEnabled) external onlyRole(PAUSER_ROLE)`. `tranche == address(0)` fans out to all three wired vaults; otherwise targets a single tranche and reverts `InvalidTranche` for any unwired address via the chained `_actionsOf` → `_kindOf` call. Body delegates to internal `_setActionStatesInner` which performs the idempotent flag comparison.
  - Amended `deposit(...)`: after `_requireSupported(token)` and before forwarding to `_strategy.deposit`, added `if (!_actionsOf(msg.sender).isDepositEnabled) revert DepositsDisabled(msg.sender);`. Keyed on `msg.sender` (already validated by `onlyTranche`), so `_actionsOf` can't revert `InvalidTranche`. `onlyTranche nonReentrant` modifiers unchanged.
  - Amended `withdraw(...)`: added the `onlyTranche` modifier (previously missing on the stub) and the inline `WithdrawalsDisabled` check; body still falls through to `revert NotImplemented()`. `nonReentrant` deferred to the real withdraw spec. Replaced the unnamed `/*tranche*/` etc. parameter syntax with named parameters and the `tranche; token; tokenAmount; baseAssets; owner_; receiver;` unused-suppression pattern to keep compile clean.
  - Updated the storage-layout `@dev` NatSpec on `PrimeCDO` to show the three new struct slots and `__gap[47]`.
  - Pause check is intentionally **CDO-only** — not duplicated on `Tranche`. Accepted trade-off (per spec §53): a paused-tranche user call burns ~150k gas in the Tranche flow before reverting inside `cdo.deposit(...)`. Tracked in Open Questions.
  - Post-deploy default: every tranche's `(isDepositEnabled, isWithdrawEnabled)` is `(false, false)` (storage zero). A `PAUSER_ROLE` holder must explicitly call `setActionStates` before user flows can succeed. Until the `AccessControlManager` exists and the role is granted, the protocol is effectively frozen — same caveat as spec 07a.
  - `pnpm build` compiles cleanly under solc 0.8.35. Remaining pure-mutability warnings are now on the four read-only stubs (`totalAssets`, both `maxWithdraw` overloads, `maxDeposit`) — the `withdraw` stub no longer warns because it now reads storage via `_actionsOf`. `updateAccounting()` is still a non-view stub.

## In Progress

- None.

## Next Up

- Implement the token-routed `withdraw(address,uint256,address,address)` and `redeem(address,uint256,address,address)` flows on `Tranche` (currently the two remaining `NotImplemented()` stubs). Spec to be added under `context/features-specs/`.
- Replace remaining `PrimeCDO` stubs with real logic, one `ICDO` surface at a time (suggested order: `updateAccounting` → `totalAssets` → `maxDeposit` / `maxWithdraw` → `withdraw` routing). Each needs its own spec.
- Stand up a mock/stub Strategy in `test/mocks/` to exercise `PrimeCDO.deposit(...)` end-to-end (`config` → `Tranche.configure` → `cdo.deposit` → `strategy.deposit`). The real Strategy contract still needs its own spec, but a mock unblocks integration coverage of the deposit path landed in spec 06.
- Build the real `Accounting` contract behind `IAccounting` (TVL refresh, yield split, loss waterfall, Senior target index). Will inherit `CDOComponent` so its back-reference passes `PrimeCDO.config(...)`'s `_requireBackRef`.
- Flesh out `IStrategy` beyond the two conversion helpers used by deposits (TVL reporting, withdrawal paths, supported-token registry). The eventual concrete `Strategy` must inherit `CDOComponent` so its back-reference passes `PrimeCDO.config(...)`.
- Resolve how preview/max/convert view functions should "use fresh accounting state" (Open Question carried over from spec 03).
- Decide on access control surface beyond `OwnableUpgradeable` (e.g. `KEEPER` for `updateAccounting`, `TRANCHE_ROLE` for deposit/withdraw callers) before the corresponding stubs get real bodies.

## Open Questions

- `context/code-standards.md` still states `pragma solidity 0.8.26;` while the codebase has been aligned to `0.8.35`. Update the standards file to match.
- Storage gap (`uint256[50] __gap`) is not yet reserved on `Tranche`. Required by `code-standards.md` for upgradeable base contracts — should it be added now or when subclasses are introduced? (`CDOComponent` reserves `uint256[49]`.)
- Upgrade pattern (UUPS vs Transparent) not yet selected for `Tranche`; `architecture-context.md` says "pick one and document".
- `IPrimeVault` token-routing `withdraw(address,...)` and `redeem(address,...)` on `Tranche` are still `NotImplemented()` stubs (the deposit/mint variants were implemented in spec 04). Coverage-aware withdraw spec still pending.
- Spec 04 uses a string `require(..., "MetaVaultExceededMaxWithdraw")` for the source-vault capacity check, which violates the project's "no string reverts — use custom errors" rule in `context/code-standards.md`. Implemented verbatim as written; should be converted to a custom error (e.g. `error MetaVaultExceededMaxWithdraw(uint256 available, uint256 required);`) and the spec amended.
- `OnMetaDeposit` is declared locally on `Tranche` rather than on `IPrimeVault` / `ITranche`. If integrators need to observe it via the interface, lift the declaration into `IPrimeVault`.
- New `IStrategy` interface is intentionally minimal — only `convertToAssets` / `convertToTokens`. Full strategy responsibilities (staking, TVL reporting, withdrawal paths, supported-token registry, gain settlement) are not yet specified.
- Spec `03-override-core.md` requires preview/max/convert functions to "use fresh accounting state", but they are `view` in `IERC4626`/`ERC4626Upgradeable` and Solidity's mutability rules forbid loosening `view` to non-view via override. They are therefore NOT overridden in this pass. Options: (a) document that callers must invoke `updateAccounting()` via multicall/sim before reading, (b) add parallel non-view variants, (c) amend the spec.
- `PrimeCDO.config(...)` enforces back-references on all five components via `ICDOComponent.getCDOAddress()`. `Tranche.initialize(IERC20,string,string,ICDO)` already sets `cdo`, so a freshly-initialized tranche passes the check. The real `Accounting` and `Strategy` contracts don't exist yet — they must inherit `CDOComponent` and have their `cdo` field set (either at initialize or via a future `setCDO`) before `config(...)` can succeed end-to-end.
- `PrimeCDO` exposes an `accounting()` getter, but `ICDO` does not declare one (only `strategy()`). Per spec 05's open question: add `function accounting() external view returns (IAccounting);` to `ICDO.sol` for symmetry, in a separate spec.
- `IAccounting` is intentionally minimal (only `updateAccounting()` and `totalAssets(address)`). The full accounting surface — exchange rates, Senior target index, yield-split outputs, loss-waterfall hooks, APR feed inputs — is deferred to a dedicated Accounting spec.
- Upgradeability pattern is still globally undecided. `PrimeCDO` does NOT inherit `UUPSUpgradeable` in this pass; adding it later is a forward-compatible inheritance change provided storage layout is preserved.
- Spec 06 calls for `ReentrancyGuardUpgradeable` from `@openzeppelin/contracts-upgradeable/utils/`, but OZ 5.6.1 contracts-upgradeable no longer ships that file (the upgradeable variant was dropped in favor of the namespaced-storage non-upgradeable `ReentrancyGuard`). Implementation substituted `ReentrancyGuard` from `@openzeppelin/contracts/utils/ReentrancyGuard.sol`, which is proxy-safe in OZ 5.x via ERC-7201. The spec should be amended to reference the actual file, or the OZ dep should be downgraded if upgradeable variant is required.
- `PrimeCDO.config(...)` now calls `_jrVault.configure()` / `_mezzVault.configure()` / `_srVault.configure()`, but does NOT revoke stale Strategy allowances from the previous strategy on re-config. If `config(...)` is re-called with a new strategy, the old strategy retains the unlimited allowance until/unless it's explicitly cleared. Decide whether to revoke per-token before re-priming.
- Token-support is currently validated in three places along the deposit path: optimistically inside `cdo.strategy().convertToAssets/convertToTokens` (Tranche meta-vault path, spec 04), explicitly via `_requireSupported` in `PrimeCDO.deposit(...)` (spec 06), and (future) inside `Strategy.deposit(...)`. Consolidate to a single authoritative check once the real Strategy lands.
- `IStrategy.deposit(address from, address token, uint256 amount)` currently returns nothing. If callers need the staked amount or share, widen the signature in a follow-up spec.
- `baseAssets` parameter on `ICDO.deposit(...)` is currently accepted-and-ignored by the CDO body. Confirm the Accounting hook (future spec) consumes it; if not, consider dropping it from the interface.
- `AccessControlManager` implementation is not yet ported. Until it is ported and deployed, the `acm_` argument to `PrimeCDO.initialize(...)` must point at a deployed contract that satisfies `IAccessControlManager`. Tests will use a mock.
- Deploy script must be updated to supply both `owner_` and `acm_` to the new 2-arg `PrimeCDO.initialize(address,address)` signature. The previous 1-arg form is gone.
- Owner-transfer flow is now 2-step (`Ownable2StepUpgradeable`): `transferOwnership(newOwner)` only proposes; the new owner must call `acceptOwnership()`. Operational tooling and runbooks must be updated to handle the second step (and to reflect the new `OwnableInvalidOwner(address)` revert selector that replaced the local `ZeroAddress()` on the owner check).
- Several OZ-style role constants imported via `AccessControlled` (`PAUSER_ROLE`, `UPDATER_CDO_APR_ROLE`, `UPDATER_FEED_ROLE`, `UPDATER_STRAT_CONFIG_ROLE`, `RESERVE_MANAGER_ROLE`, `COOLDOWN_WORKER_ROLE`, `PROPOSER_CONFIG_ROLE`) are not yet wired to any Atrium function. Inherited verbatim from Strata to preserve a shared role vocabulary; track which subset Atrium actually uses as new role-gated functions are introduced.
- Spec 07a port carries Strata's `onlyTwoStepConfigManager` modifier that uses a string `require(..., "ConfigManagerOnly")`, violating the codebase's "no string reverts — use custom errors" rule. Ported verbatim per the spec's "port verbatim" mandate; should be converted to a custom error in a follow-up.
- Spec 07a's "port verbatim" mandate again hit the missing `ReentrancyGuardUpgradeable` in OZ 5.6.1 (same issue as spec 06). Substituted the non-upgradeable `ReentrancyGuard` in `AccessControlled.sol` and removed the `__ReentrancyGuard_init()` call from `AccessControlled_init`. The spec's storage-layout NatSpec was reworded to reflect that the non-upgradeable variant uses ERC-7201 namespaced storage (no sequential slots, no init).
- Spec 07b pause check is CDO-only (no Tranche-level early revert). A paused-tranche user call wastes ~150k gas through the full Tranche flow (`updateAccounting`, strategy conversion, `previewDeposit`, `safeTransferFrom`, `_mint`) before reverting inside `cdo.deposit(...)`. Acknowledged trade-off; revisit if pause becomes a frequent operational lever.
- Spec 07b grants no `PAUSER_ROLE` because `AccessControlManager` is not yet deployed (carried from 07a). Until role is granted, no caller can call `setActionStates`, so the protocol stays in its disabled-by-default state — effectively frozen until 07a's `AccessControlManager` dependency lands.
- StrataCDO has an auto-pause `shortfallPauser` triggered after every deposit/withdraw. Atrium does not replicate this in spec 07b — confirm whether Atrium needs the equivalent and spec it separately if so.
- `TrancheKind` enum currently lives on `PrimeCDO` only. Promote to a shared interface (e.g. `ICDO` or a new `ITrancheKind` library) if other contracts begin to need the same three-way identifier.

## Architecture Decisions

- Use OpenZeppelin upgradeable contracts (`@openzeppelin/contracts-upgradeable`) for the tranche base.
- Tranches inherit `ERC4626Upgradeable` directly; tranche-specific withdrawal semantics will be layered via overrides rather than a custom accounting rewrite.
- No constructors on tranche contracts — initialization happens through the `initializer`-guarded `initialize(...)` function.
- No access control or tranche business logic in the base contract at this stage (deferred).
- `PrimeCDO` lives at `contracts/core/PrimeCDO.sol`. It does NOT inherit `CDOComponent` (it is the CDO itself, not a component pointing back to one). Components reference `PrimeCDO` via `CDOComponent.cdo`; `PrimeCDO` verifies the back-reference at wire time.
- `PrimeCDO` uses `OwnableUpgradeable` for admin gating in this phase. Component wiring is atomic via `config(...)` (no partial setters) and is re-callable to allow re-wiring under the same owner.
- `IAccounting` and `IStrategy` are intentionally minimal — only what `PrimeCDO` and `Tranche` currently call. Full surfaces are deferred to their own specs.
- Deposit token-flow pattern (spec 06): the Tranche pre-approves the Strategy unlimited via `Tranche.configure()` (driven by `PrimeCDO.config(...)`), and the Strategy pulls tokens directly from the Tranche during `cdo.deposit(...)`. CDO is orchestration-only — it never holds the deposited tokens and never sets its own allowances. Unlimited Tranche→Strategy allowance is bounded by `Strategy.onlyCDO`, so only CDO can trigger the pull. CDO emits no event on deposit (Tranche emits `Deposit` / `OnPrimeDeposit`; Strategy will emit its own once implemented).
- `PrimeCDO` uses the external-registry access-control pattern via `AccessControlled` + `IAccessControlManager` shared with Strata. Role state is not embedded in `PrimeCDO`; `onlyRole(...)` (and `_checkAccessAllowed(bytes4)`) delegate to the external `acm` contract. Same pattern is intended for every Atrium contract that needs role-gated functions. Owner gating is now 2-step (`Ownable2StepUpgradeable`): `transferOwnership(newOwner)` proposes, the new owner must call `acceptOwnership()`.
- `PrimeCDO`'s storage-layout baseline shifted on 2026-05-21 via spec 07a (Initializable → Ownable → Ownable2Step → ReentrancyGuard (non-upgradeable, ERC-7201) → AccessControlled (acm + twoStepConfigManager + __gap[48]) → PrimeCDO own (5 slots + __gap[50])). Further amended on 2026-05-21 via spec 07b: PrimeCDO own tail extended to `5 slots + actionsJr/actionsMezz/actionsSr (3 packed-bool slots) + __gap[47]`. All future upgrades start from this layout. No `PrimeCDO` proxy was live before either shift.
- Pause is gated by `PAUSER_ROLE` (delegated to the external `AccessControlManager` via `AccessControlled`) and stored per-tranche on `PrimeCDO` as `TActionState` structs (one per Junior/Mezzanine/Senior). Default DISABLED on every tranche/category, mirroring StrataCDO — forces a deliberate enable step post-deploy and is audit-friendly.
- Pause checks are performed only at CDO level (in `CDO.deposit(...)` and `CDO.withdraw(...)`), not at Tranche level. Accepted trade-off: a paused-tranche user call burns ~150k gas through the Tranche flow before the CDO reverts. Re-evaluate if pause becomes a frequent operational lever.

## Session Notes

- 2026-05-21: Implemented spec `context/features-specs/01-create-tranche.md`. Files added: `contracts/interfaces/ITranche.sol`, `contracts/vaults/Tranche.sol`. Dependency added: `@openzeppelin/contracts-upgradeable@^5.6.1`.
- 2026-05-21: Implemented spec `context/features-specs/02-create-cdo-interface.md`. File added: `contracts/interfaces/ICDO.sol` (interface only, no implementation). Build currently fails due to the pre-existing pragma mismatch between `0.8.35` in the tranche/CDO sources and solc `0.8.28` in `hardhat.config.ts` (already tracked in Open Questions); `ICDO.sol` inherits the same pragma as `ITranche.sol` for consistency.
- 2026-05-21: Aligned the toolchain to solc `0.8.35`. Bumped `hardhat.config.ts` (`0.8.28` → `0.8.35`) and `contracts/base/CDOComponent.sol` (`^0.8.28` → `0.8.35`). Cleaned up `contracts/vaults/Tranche.sol`: removed redundant `ERC20Upgradeable` / `Initializable` from the inheritance list (both already brought in transitively via `ERC4626Upgradeable`), reordered as `CDOComponent, ERC4626Upgradeable, ITranche`. Added stub overrides for the four `IPrimeVault` token-routing functions (`deposit/mint/withdraw/redeem(address token, ...)`) that revert with a local `NotImplemented()` error, so `Tranche` is now a concrete (non-abstract) contract pending the real multi-asset implementation. `pnpm build` compiles cleanly under solc 0.8.35 (only "state mutability can be restricted to pure" warnings on the four stubs — expected and will disappear once they have real bodies).
- 2026-05-21: Implemented spec `context/features-specs/03-override-core.md`. Overrode the four state-changing ERC4626 entrypoints on `contracts/vaults/Tranche.sol` (`deposit(uint256,address)`, `mint(uint256,address)`, `withdraw(uint256,address,address)`, `redeem(uint256,address,address)`) to call `cdo.updateAccounting()` before delegating to `super.*`. Each carries `override(ERC4626Upgradeable, IERC4626)` because the signatures are inherited from both bases; added a named import for `IERC4626` from `@openzeppelin/contracts/interfaces/IERC4626.sol`. The view-only preview/max/convert functions were intentionally NOT overridden — Solidity disallows loosening `view` to non-view via override, so calling `cdo.updateAccounting()` from them is impossible (tracked as a new Open Question). `pnpm build` compiles cleanly under solc 0.8.35; the only remaining warnings are the pre-existing "state mutability can be restricted to pure" notes on the four `IPrimeVault` token-routed stubs.
- 2026-05-21: Scaffolded `contracts/PrimeCDO.sol` (`contract PrimeCDO is Initializable, ICDO`). Exposes public `jrVault`/`mezzVault`/`srVault`/`strategy` storage (auto-implementing the four ICDO getters), and reverts `NotImplemented()` from every other ICDO function. Reserved a `uint256[46]` storage gap. No access control, no business logic — just the surface. (Originally created as `CDO`; renamed to `PrimeCDO` in the same session. The initial `initialize(ITranche,ITranche,ITranche,IStrategy)` was also removed in-session — wiring will move to dedicated config functions per the user's direction.) `pnpm build` compiles cleanly under solc 0.8.35.
- 2026-05-21: Implemented spec `context/features-specs/04-deposit-flow.md`. Replaced the `deposit(address,uint256,address)` and `mint(address,uint256,address)` stubs on `contracts/vaults/Tranche.sol` with the spec's native + meta-vault flows, and added a new internal `_deposit(address token, address caller, address receiver, uint256 baseAssets, uint256 tokenAssets, uint256 shares)` carrying the maxWithdraw check, `SafeERC20.safeTransferFrom` pull, `_mint`, `cdo.deposit(...)` forwarding, and `Deposit` + `OnMetaDeposit` event emissions. To compile the spec's literal `cdo.strategy().convertToAssets/convertToTokens` calls I added `function strategy() external view returns (IStrategy);` to `contracts/interfaces/ICDO.sol` and created `contracts/interfaces/IStrategy.sol` with just the two conversion helpers (full strategy surface deferred to a future spec). Imports added to `Tranche.sol`: `SafeERC20`, `Math`. `OnMetaDeposit` is declared locally on `Tranche`. `pnpm build` compiles cleanly under solc 0.8.35; only the two pre-existing pure-warning lints on the remaining `withdraw(address,...)` / `redeem(address,...)` stubs remain.
- 2026-05-21: Implemented spec `context/features-specs/05-create-cdo.md`. Files added: `contracts/interfaces/IAccounting.sol` (minimal: `updateAccounting()` + `totalAssets(address)`), `contracts/core/PrimeCDO.sol` (replaces the previous `contracts/PrimeCDO.sol` scaffold, which was deleted). New `PrimeCDO is Initializable, OwnableUpgradeable, ICDO`: internal storage for `_jrVault`/`_mezzVault`/`_srVault`/`_accounting`/`_strategy` + `uint256[50] __gap`; constructor calls `_disableInitializers()`; `initialize(address owner_)` is `initializer`-gated and reverts `ZeroAddress()` on a null owner; atomic owner-gated `config(jr, mz, sr, accounting_, strategy_)` runs five zero-address checks and five back-reference checks via `ICDOComponent.getCDOAddress()` before writing storage and emitting `Configured`. Custom errors: `NotImplemented`, `ZeroAddress`, `InvalidComponent(component, expectedCDO, actualCDO)`. All remaining `ICDO` functions revert `NotImplemented()`. `pnpm build` compiles cleanly under solc 0.8.35; warnings remaining are the pre-existing pure-mutability notes on the seven `PrimeCDO` stubs and the two `Tranche` withdraw/redeem stubs.
- 2026-05-21: Implemented spec `context/features-specs/06-cdo-deposit.md`. Files modified: `contracts/interfaces/IStrategy.sol`, `contracts/interfaces/ITranche.sol`, `contracts/vaults/Tranche.sol`, `contracts/core/PrimeCDO.sol`. Adopted Pattern B/3 (Strategy pulls from Tranche): Tranche pre-approves Strategy unlimited per supported token in `Tranche.configure()` (`onlyCDO`-gated, idempotent, uses `SafeERC20.forceApprove`); CDO stays orchestration-only and holds no tokens; Strategy's `onlyCDO` (future spec) bounds the unlimited allowance. `PrimeCDO.config(...)` was amended to call `configure()` on each of the three wired tranches after storage writes (order matters — `_strategy` is set first because `Tranche.configure()` reads `cdo.strategy()`). `PrimeCDO.deposit(...)` is now `external override onlyTranche nonReentrant`; validates `token` via new `_requireSupported(address)` helper (O(n) over `_strategy.getSupportedTokens()`, n bounded by Strategy admin) and forwards exactly one external call: `_strategy.deposit(msg.sender, token, tokenAmount)`. No CDO event emitted; no CDO storage written. New custom errors: `UnauthorizedTranche(address caller)`, `TokenNotSupported(address token)`. Reentrancy: spec called for `ReentrancyGuardUpgradeable` from `@openzeppelin/contracts-upgradeable/utils/`, but OZ 5.6.1 no longer ships that file — substituted the non-upgradeable `ReentrancyGuard` from `@openzeppelin/contracts/utils/ReentrancyGuard.sol`, which is proxy-safe in OZ 5.x via ERC-7201 namespaced storage and needs no initializer (tracked in Open Questions). `__gap` unchanged at 50. `pnpm build` compiles cleanly under solc 0.8.35; remaining warnings are the pre-existing pure-mutability lints on six `PrimeCDO` stubs and the two `Tranche` withdraw/redeem stubs.
- 2026-05-21: Implemented spec `context/features-specs/07a-access-controlled.md`. Files added: `contracts/interfaces/IAccessControlManager.sol`, `contracts/governance/AccessControlled.sol` (both ported verbatim from Strata: pragma `^0.8.28` → `0.8.35`, license `UNLICENSED` → `BUSL-1.1`, Strata import path adjusted). One substitution forced by OZ 5.6.1: `ReentrancyGuardUpgradeable` does not ship, so `AccessControlled` inherits the non-upgradeable `ReentrancyGuard` (ERC-7201 namespaced storage; no sequential slots; no init step) and the `__ReentrancyGuard_init()` call is removed from `AccessControlled_init`. File amended: `contracts/core/PrimeCDO.sol` — inheritance collapsed to `AccessControlled, ICDO`; removed local constructor (`_disableInitializers()` inherited), removed local `error ZeroAddress()` (inherited); `initialize(address)` → `initialize(address,address)` with body `AccessControlled_init(owner_, acm_)`; added `@dev` storage-layout NatSpec documenting the post-07a baseline. `config()` still `onlyOwner`; `deposit()` still `onlyTranche nonReentrant`; all other `ICDO` functions still revert `NotImplemented()`. Storage-layout-breaking by design (no `PrimeCDO` proxy is live yet, confirmed 2026-05-21) — the new layout is the upgrade baseline. Rationale for verbatim port: Strata source is audited; verbatim port avoids a custom audit scope and preserves the shared role-constant vocabulary across protocols. `pnpm build` compiles cleanly under solc 0.8.35; warnings remaining are the pre-existing pure-mutability lints on six `PrimeCDO` stubs and the two `Tranche` withdraw/redeem stubs.
- 2026-05-21: Implemented spec `context/features-specs/07b-pause-action.md`. File amended: `contracts/core/PrimeCDO.sol` only (no new files, no interface changes). Added `enum TrancheKind { JUNIOR, MEZZANINE, SENIOR }` and `struct TActionState { bool isDepositEnabled; bool isWithdrawEnabled; }` (two bools pack into one slot). Added three `public` storage variables `actionsJr` / `actionsMezz` / `actionsSr` between `_strategy` and `__gap`; shrank `__gap` from 50 → 47 to keep total reserved at 50. New events `DepositsStateChanged(address indexed tranche, bool enabled)` and `WithdrawalsStateChanged(address indexed tranche, bool enabled)` — emitted only on actual flag flip (idempotent). New errors `InvalidTranche(address)`, `DepositsDisabled(address)`, `WithdrawalsDisabled(address)`. New helpers `_kindOf(address)` (three-way address compare, reverts `InvalidTranche` on unwired addresses) and `_actionsOf(address)` (returns `TActionState storage` reference; declared `view` because the helper itself performs no writes — Solidity 0.8.35 allows it). New external `setActionStates(address tranche, bool d, bool w) onlyRole(PAUSER_ROLE)`; `tranche == address(0)` fans out to all three vaults. Wired `if (!_actionsOf(msg.sender).isDepositEnabled) revert DepositsDisabled(msg.sender);` into `deposit(...)` after `_requireSupported(token)`. Wired the equivalent withdraw check into `withdraw(...)` and added the missing `onlyTranche` modifier — body still falls through to `revert NotImplemented()`. `nonReentrant` on `withdraw` deferred to the real withdraw spec. Storage-layout `@dev` NatSpec updated to show the three new slots and `__gap[47]`. Default post-deploy: every `(isDepositEnabled, isWithdrawEnabled)` is `(false, false)`; user flows are frozen until a `PAUSER_ROLE` holder explicitly enables them (requires 07a's `AccessControlManager` to be deployed and the role granted — same caveat carried from 07a). Storage-layout-breaking by design (no `PrimeCDO` proxy live yet). `pnpm build` compiles cleanly under solc 0.8.35; remaining pure-mutability warnings on `totalAssets`, both `maxWithdraw` overloads, and `maxDeposit` stubs (`withdraw` no longer warns because it now reads storage via `_actionsOf`).
